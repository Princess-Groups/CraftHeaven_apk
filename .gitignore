# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment / secrets — NEVER commit these
.env
.env.*
!.env.example

# Vercel local build output (Vercel builds from source on its own infra)
.vercel/

# Prebuilt Android artifacts (build them locally; not part of the web deployment)
release/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Android / Capacitor build artifacts (native wrapper for the Play Store)
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
keystore/
keystore.properties
*.keystore
*.jks

# Capacitor
capacitor.config.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*
